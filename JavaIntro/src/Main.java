public class Main {
    /**
     * How Java works:
     * Each file in Java is a class and they have to have the same name with the class you want to implement.
     * In order to run a class, you have to have a "main" method that takes in the .
     * */
    //method (function) declare here
    //method has to be declared outside of main function
    /**
     * this method takes in an integer and print it out
     * @param a
     */
    public static void printInt(int a) {

    }

    public static void main(String[] args) {
        //variable declaration
        int a = new Integer(1);
        //print
        System.out.println("Hello world!");
        //loop
        for (int i = 0; i < 10; i++) {
            System.out.println(i);
        }
        //if

        //I/O

        //Operators (String comparison and logic)
    }
}