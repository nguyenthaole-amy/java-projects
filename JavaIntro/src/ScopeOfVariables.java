public class ScopeOfVariables {
    /*
    * Instance variables are variables that lies in the class but not in a method.
    * In python, the equivalent of this is a global variable that can be used by every method.
    * To declare a variable in Java you need to follow the format [TYPE] varName = new [TYPE];
     */
    //array of integers

    /*
    * Java allows you to have a variable without having any value in it, we can declare this by
    * following the format [TYPE] varName;
    */
}
